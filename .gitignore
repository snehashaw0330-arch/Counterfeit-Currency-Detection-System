venv/
dataset/
.ipynb_checkpoints/
__pycache__/
*.pyc

# Frontend
node_modules/
.next/
out/

# Tests / IDE
.pytest_cache/
.vscode/
.idea/

# Claude Code session data
.claude/

# Backend debug crops
backend/*.jpg
tests/diagnostic_report.json
tests/ocr_debug/

# Phase D classical model binaries (regenerable: scripts/train_classical.py).
# metrics.json is kept tracked as the committed benchmark record.
models/classical/*.joblib

# Phase F experimental CNN (not adopted; regenerable via scripts/train_cnn.py)
models/*_v2.keras

# Scratch eval/calibration console dumps (regenerable via the scripts)
eval_out.txt
calib_out.txt
